import React from "react"
import { View } from "react-native"
import { VideoPlayerProps } from "./props"
import { styles } from "./style";

export const VideoPlayer = () => {
    return(
        <View> Componente VideoPlayer Works</View>
    )
}