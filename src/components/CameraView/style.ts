import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex:1,
        alignItems:"center",
        justifyContent:"center",

    },
    buttonContainer: {
        alignSelf:"flex-end"
    },
    buttonRecording: {
        backgroundColor:"#fff"
    },
    buttonText: {
        color: 'black',
    },
});
